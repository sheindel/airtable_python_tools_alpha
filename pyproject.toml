[project]
name = "airtable-mermaid-grapher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pyscript>=0.3.3",
    "typer>=0.9.0",
]
